// CMake trash  82889182187278.h: archivo de inclusión para archivos de inclusión estándar del sistema,
// o archivos de inclusión específicos de un proyecto.

#pragma once

#include <iostream>
#include <random>

#include "petal/base.h"

petal::ia iaa;


/*
class MyClass
{
public:
	MyClass();
	~MyClass();
	int rando();
	string ao[9];
	
	string cpuprint() {
		return ao[rando()];
	}
	
	void cpuleer(string texto) {
		ao[tamaño_c] = texto;
		if (tamaño_c < tamaño) { tamaño_c = 0; }
		else {
			tamaño_c++;
		}
	}
	
private:
	int tamaño = size(ao);
	int tamaño_c = 0;
};

MyClass::MyClass()
{
	
}

MyClass::~MyClass()
{
} 
int MyClass::rando() {
	random_device rd;
	mt19937 gen(rd());
	uniform_int_distribution<> eleg(0, tamaño);
	int num = eleg(gen);
	if (ao[num] == NULL) {
		return 0;
	}
	else {
		return eleg(gen);
	}
}
  


template <typename morg>
morg testee() { return "o"; }
*/

// TODO: Haga referencia aquí a los encabezados adicionales que el programa requiere.
